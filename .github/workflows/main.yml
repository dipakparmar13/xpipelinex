name: Upload Website

on:
  push:
    branches:
    - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@main
    - run: |
      
        - rsync -zhvr . ubuntu@52.90.58.254:/home/ubuntu/dipak/
        - echo "dipak new code"