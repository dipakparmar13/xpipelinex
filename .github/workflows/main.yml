name: Upload Website

on:
  push:
    branches:
    - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@main
    - run: |
        - aws rsync . ubuntu@52.90.58.254:/home/ubuntu/dipak/
        - echo "file sync"
        - echo "dipak new code"